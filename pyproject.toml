[project]
name = "bos-tooling"
version = "0.0.2"
description = "Validator tooling for the experimental BOS typed decision graph"
requires-python = ">=3.11"
dependencies = [
  "jsonschema==4.25.1",
  "PyYAML==6.0.3",
]

[tool.uv]
package = false
